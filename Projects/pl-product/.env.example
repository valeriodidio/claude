# .env.example — committare questo, NON il .env reale
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=reports_reader
DB_PASSWORD=dev_password_change_me
DB_NAME=smart2

# Credenziali root Docker (usate da seed_from_prod.py per TRUNCATE/INSERT)
DB_ROOT_PASSWORD=root_dev

APP_HOST=127.0.0.1
APP_PORT=8000
APP_RELOAD=true

INTERNAL_TOKEN=
ALLOWED_ORIGINS=

ENABLE_STATIC_TEST_UI=true
LOG_LEVEL=INFO
